Act as a Senior Full Stack Developer. Implement 2 High-Impact UI/UX Features to finalize the project.

### FEATURE 1: Professional Admin Dashboard with Charts (Visual Analytics)
Transform the Admin Dashboard into a visual command center using **Chart.js**.

**1. Backend Updates (`app.py` -> `admin_dashboard` route):**
   - Calculate KPIs:
     - `total_sales`: Sum of all completed orders.
     - `total_orders`: Count of all orders.
     - `total_sellers`: Count of approved sellers.
     - `total_products`: Count of all products.
   - Prepare Chart Data:
     - `category_data`: Count of products per category (e.g., {"Gübre": 10, "Tohum": 5}).
     - `sales_data`: (Mock or Real) Sales volume for the last 7 days.
   - Pass these variables to the `admin/dashboard.html` template.

**2. Frontend Updates (`templates/admin/dashboard.html`):**
   - **Import:** Add `<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>` at the top.
   - **Layout (Grid System):**
     - **Row 1 (KPI Cards):** 4 Colorful Cards (Bootstrap `card`) displaying the KPIs with FontAwesome icons (e.g., `fa-wallet`, `fa-shopping-cart`, `fa-users`, `fa-box`).
       - Style: Use gradients (e.g., Linear Gradient Orange for Sales, Green for Sellers).
     - **Row 2 (Charts):**
       - Col-6: A **Pie Chart** showing "Product Distribution by Category".
       - Col-6: A **Bar/Line Chart** showing "Weekly Sales Performance".
   - **Script:** Initialize the Chart.js instances with the data passed from the backend.

---

### FEATURE 2: Visual Order Tracking Timeline (Customer UX)
Enhance the Customer Order Detail page to show a visual progress bar instead of just text status.

**1. CSS Updates (`static/css/style.css`):**
   - Create a class `.track-order`.
   - Design a horizontal step wizard with 4 steps:
     1. **Sipariş Alındı** (Icon: `fa-file-invoice`)
     2. **Hazırlanıyor** (Icon: `fa-box-open`)
     3. **Kargoya/Ambara Verildi** (Icon: `fa-truck`)
     4. **Teslim Edildi** (Icon: `fa-check-circle`)
   - **Active State:** Steps should be GREEN (`#2E7D32`) if completed, GRAY (`#ddd`) if pending.
   - **Connector:** Draw a line between circles. The line should be green if the step is passed.

**2. Frontend Updates (`templates/order_detail.html` or `templates/buyer/orders.html`):**
   - Insert the Timeline HTML structure at the top of the order detail card.
   - **Logic:** Use Jinja2 `if` statements to add the `active` class to steps based on `order.status`.
     - *Example:* If status is 'shipped', then steps 1, 2, and 3 are active.

**Result:**
- The Admin Dashboard should look like a professional analytics page with graphs.
- The Customer Order page should look like a modern e-commerce tracking screen.